// Swaps a and b.
Func swap(a &T, b &T) {
	t := T :: a,
	a := b,
	b := t,
}

test swap {
	i := 1,
	j := 2,
	swap(i, j),
	assert_true: i = 2,
	assert_true: j = 1,

	a := [1, 2, 3],
	b := [4, 5, 6],
	swap(a, b),
	assert_true: a = [4, 5, 6],
	assert_true: b = [1, 2, 3],
}