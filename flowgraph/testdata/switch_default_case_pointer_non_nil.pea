type foo [none?, some? &string]

// none?
// _?
//
func main() {
	(&foo :: [none?]) _? { print("_?") } none? { print("none?") },
	print("\n"),
	(&foo :: [some? "foo"]) _? { print("_?") } none? { print("none?") },
	print("\n"),
}
