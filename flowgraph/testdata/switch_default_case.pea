type foo [a? string, b?, c? string, d?, e?]

// a? foo
// b?
// _?
// _?
// e?
//
func main() {
	print_foo([a? "foo"]),
	print("\n"),
	print_foo([b?]),
	print("\n"),
	print_foo([c? "foo"]),
	print("\n"),
	print_foo([d?]),
	print("\n"),
	print_foo([e?]),
	print("\n")
}

func print_foo(f foo) {
	f a? (s string){
		print("a? "),
		print(s)
	} b? {
		print("b?")
	} _? {
		print("_?")
	} e? {
		print("e?")
	}
}
